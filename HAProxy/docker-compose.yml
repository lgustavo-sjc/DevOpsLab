#version: '3.8'

services:
  # consul:
  #   image: hashicorp/consul:1.19  # Fixando a versão para evitar incompatibilidades
  #   container_name: consul-server
  #   ports:
  #     - "8500:8500"
  #     - "8600:8600/udp"
  #     - "8301:8301"
  #     - "8501:8501"
  #   command: "agent -config-file=/consul/config/consul-config.json"
  #   volumes:
  #     - consul_data:/consul/data
  #     - ./consul-config.json:/consul/config/consul-config.json
  #   networks:
  #     - consul_network

  haproxy:
    image: haproxy:3.1.2
    container_name: haproxy-loadbalancer
    ports:
      - "8080:8080"
      - "8989:8989"
      - "6012:6012"
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg
    # depends_on:
    #   - consul
    networks:
      - consul_network

# volumes:
#   consul_data:

networks:
  consul_network:
    driver: bridge
